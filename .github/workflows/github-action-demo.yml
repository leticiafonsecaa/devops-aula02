name: GitHub Actions Demo
run-name: ${{ github.workflow }} is testing out GitHub Actions
on: [push]
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - name: Primeiro Step do fluxo
        run: echo "The job was automatically triggered by a ${{ github.event_name }} event."
      - name: Segundo Step do fluxo
        run: echo "The job was automatically triggered by a ${{ github.event_name }} event."
      - name: Terceiro Step do fluxo
        run: echo "O repositório que está sendo utilizado é ${{ github.repository }}."
      - name: Ultimo Step do fluxo
        run: echo "Criado em 14/09/2026."